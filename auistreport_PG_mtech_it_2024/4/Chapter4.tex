% Chapter 4

\chapter{\uppercase{Design/ Pseudocode/ Algorithms/ Logic}} % Main chapter title
\label{chap4} % For referencing
This chapter is optional depending on the type of project. The format of the sample algorithm is given below.

\section{\uppercase{Algorithm 1}}
\begin{algorithm}
\begin{algorithmic}[1]
\State Get the number of variables $num$ 
\State Start with an empty list $x$ $[~]$
\For {each $n$ of $num$}
	\State Get the $x$
	\State  Get the $t$
	\State Get the $w$
\EndFor 
\If {$xl$ == $s$} 
	\State terminate with the failure message \textbf{failed}
\Else
	\State $z = x$
\EndIf 

\end{algorithmic}
\end{algorithm}



